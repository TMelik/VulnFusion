[pytest]
markers =
    live_llm: opt-in smoke tests that perform real LLM provider calls and require explicit enablement
    zap_docker: opt-in smoke tests that perform real ZAP Docker automation runs and require explicit enablement
